export interface FadMedicalIndexRequestModalInterface {
    useridin: string;
    networkId: number;

    setUserId(useridin: string): FadMedicalIndexRequestModalInterface;
    setNetworkId(networkId: number): FadMedicalIndexRequestModalInterface;
}
